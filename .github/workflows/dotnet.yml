name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.x.x
    - name: Restore dependencies
      run: dotnet restore Presentation/Presentation.csproj
    - name: Build
      run: dotnet build Presentation/Presentation.csproj --no-restore
    - name: Test
      run: dotnet test Presentation/Presentation.csproj --no-build --verbosity normal
